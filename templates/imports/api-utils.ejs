<%
/** 
* @description API Instance import 구문 생성 템플릿 입니다.
*/

const { moduleConfig, codegenConfig, resolveRelativeImportPath } = it;

const { pathInfo } = codegenConfig.customOutput;
const apiUtilsAlias = resolveRelativeImportPath(pathInfo.apiUtils.alias)
%>
import { validateSchema, createSearchParams } from '<%~ apiUtilsAlias %>';